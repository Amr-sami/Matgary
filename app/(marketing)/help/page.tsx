import { HelpContent } from "./HelpContent";

export const metadata = {
  title: "مركز المساعدة — متجري",
  description: "إجابات سريعة وشروحات لكل ما يخص استخدام متجري.",
};

export default function HelpPage() {
  return <HelpContent />;
}
