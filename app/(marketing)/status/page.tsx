import { StatusContent } from "./StatusContent";

export const metadata = {
  title: "حالة الخدمة — متجري",
  description: "حالة كل خدمات متجري — يتم تحديثها لحظياً.",
};

export default function StatusPage() {
  return <StatusContent />;
}
