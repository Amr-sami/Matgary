// STUB — owned by the feature agent. Mounted once by <AppEffects/> in the root layout.
// Render null; do work in hooks. Runs inside QueryClientProvider + SafeAreaProvider, after
// session bootstrap starts (read status from useSession).
export function SnapshotRefresher() {
  return null;
}
