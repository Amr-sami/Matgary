import { Text, View } from "react-native";
import { Screen } from "@/components/layout/Screen";

// STUB — replaced by the feature agent.
export default function SalesHistoryScreen() {
  return (
    <Screen>
      <View><Text>app/(app)/sales/history.tsx</Text></View>
    </Screen>
  );
}
