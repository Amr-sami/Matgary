import { Text, View } from "react-native";
import { Screen } from "@/components/layout/Screen";

// STUB — replaced by the feature agent.
export default function SaleDetailScreen() {
  return (
    <Screen>
      <View><Text>app/(app)/sales/[id].tsx</Text></View>
    </Screen>
  );
}
