// The 3,072-key Arabic/English dictionaries plus locale config, shared by web
// and mobile so a string is translated once. Currently still living at
// apps/web/dictionaries — moved here when the mobile app needs them.
export {};
